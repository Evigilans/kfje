apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: {{ .Values.global.namespace }}
  name: user-service-ingress
spec:
  rules:
    - host: users-kfje.com
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: user-service
                port:
                  number: 8081